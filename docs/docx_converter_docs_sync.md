# Синхронизация документации с кодом (аудит 2026-09-18)

Результат этапа 0 спек OCR-тракта (`docs/specs/ocr/00-audit.md`). Продуктовый
код не менялся — здесь только факты, перепроверенные по репозиторию, и список
мест, где `CLAUDE.md` / `README.md` / `docs/PLAN_OCR.md` разошлись с кодом.
Сами эти файлы спека 00 править не разрешает.

## Модули

Семь продуктовых модулей в корне, `conftest.py` восьмой файл — тестовая
инфраструктура, не модуль приложения. Число строк — `wc -l` на дату аудита.

| Файл | Строк | Роль |
|---|---|---|
| `convert.py` | 1630 | Markdown → DOCX, монолит |
| `app.py` | 938 | Streamlit UI, два режима (728 на дату аудита; +MinerU, ПРАВКА #73) |
| `file_converter.py` | 712 | DOCX/PDF/TXT → Markdown, слой MarkItDown, `analyze_pdf_pages` |
| `ocr_converter.py` | 185 | subprocess-обёртка `ocrmypdf` |
| `pdf_core.py` | 115 | провайдеро-независимое ядро PDF → Markdown |
| `ocr_auto_mode.py` | 81 | оркестратор «OCR или нет» |
| `markdown_cleanup.py` | 32 | детерминированная чистка OCR-Markdown |
| `conftest.py` | 23 | маркер `live`; с ПРАВКИ #71 включается флагом `MINERU_LIVE=1` |

Зависимости: `requests` в `requirements.txt` **нет**; в `.venv` он стоит
транзитивно (2.34.2), поэтому локально импортируется и без правки файла. Строку
`requests` добавляет спека 02, не эта.

`markdown_cleanup.cleanup_ocr_markdown` ни к чему не подключён (ни UI, ни
тракт), покрыт `tests/test_markdown_cleanup.py`. Из его правил для OCR-тракта
важна одна: `\bNo\.?\s*(?=\d)` → `№ ` (с пробелом). `Noп/п` она не ловит —
после `No` там буква, а не цифра.

## Нумерация правок

Последняя правка в коде — **#59**, `app.py:284`. Сплошной набор
`#1…#24, #26…#59`; **#25 отсутствует** и никогда не существовал — номер
пропущен навсегда, задним числом не присваивается. Проверка:

```bash
grep -rn "ПРАВКА #" --include=*.py . | grep -v '/\.venv/'
```

Следующий свободный номер — **#60**, он закреплён за спекой 01. Распределение
#60…#66 по спекам — в `docs/specs/ocr/README.md`.

**Состояние на ПРАВКУ #73 (2026-09-19).** Абзацы выше — снимок на дату аудита.
Сейчас в коде `#1…#24, #26…#73`: #60 — `pdf_core.py`, #61–#66 — по одной на
модуль `ocr/`, #67–#72 — последующие исправления тракта (#71 — `conftest.py`),
**#73 — `app.py`, этап 8**. Следующий свободный номер — **#74**.

## Контракт OcrProvider

`pdf_core.py:16-20`, дословно:

```python
class OcrProvider(Protocol):
    """Интерфейс OCR-провайдера: скан-PDF (bytes) -> markdown."""

    def ocr_pdf_to_markdown(self, pdf_bytes: bytes,
                            page_range: str | None = None) -> str: ...
```

Единственная реализация — `OcrmypdfProvider` (`pdf_core.py:23`). `page_range`
имеет умолчание `None`, то есть реализации обязаны принимать вызов и без него.

## Как вызывается OCR-тракт

```
app._convert_uploaded_file (app.py:245)
  └─ pdf_core.pdf_to_markdown_with_status(bytes, page_range=…, mode=…)
       → (str, dict | None)
```

Сигнатуры (`pdf_core.py:43`, `pdf_core.py:84`):

```python
def pdf_to_markdown_with_status(pdf_bytes, *, page_range=None, mode="auto",
                                provider=None) -> tuple[str, dict[str, Any] | None]
def pdf_to_markdown(pdf_bytes, *, page_range=None, mode="auto", provider=None) -> str
```

Аргумент `provider` из UI **не передаётся никогда** — `app.py` зовёт функцию
только с `page_range` и `mode`. Единственные потребители протокола на момент
аудита — тесты (`tests/test_pdf_core.py`). Поэтому подключение нового
провайдера к UI — отдельный этап 8, а не побочный эффект этапов 01–07.

`ocr_auto_mode.py` протокол провайдера не использует вообще: слов `provider` и
`Protocol` в файле нет. Этап 1 его не трогает.

### ПРАВКА #73: MinerU из UI (этап 8)

У `ocr_mode` три значения: `off`, `auto`, `mineru`. `auto` (OCRmyPDF) попадает
в переключатель, только если `check_ocr_dependencies` нашёл все три бинарника
(`app._ocr_mode_options`, проверка кэшируется на процесс) — на Streamlit Cloud
их нет, и вариант там не предлагается. Для PDF в режиме `mineru`:

```
app._convert_uploaded_file(…, ocr_mode="mineru", verify=…, annotate=…, status=…)
  ├─ app._pdf_page_subset(bytes, page_range)      # pypdf; run_pipeline диапазона не знает
  └─ ocr.cli.run_pipeline(bytes, source_name=…, work_dir=<tmp>, verify=…, annotate=…,
                          cache=LocalCache(app._OCR_CACHE_ROOT),
                          provider_factory=app._mineru_provider_factory(key, status))
       → (markdown, report)
```

- `run_pipeline` и весь `ocr/` **не менялись**. Прогресс (`st.status`) снят со
  швов провайдера: подкласс `MineruProvider` в фабрике метит `fetch_raw_zip`
  («загрузка» → «постобработка и проверка»), инжектируемый `sleep` — «ожидание».
  Отдельного колбэка прогресса в тракте нет; постобработка и проверка идут
  одной меткой (это миллисекунды).
- Ключ: `st.secrets["MINERU_API_KEY"]`, затем переменная окружения
  (`app._mineru_api_key`). При попадании в кэш ключ не нужен.
- Кэш — `LocalCache` в `.cache/ocr` рядом с `app.py`; на Streamlit Cloud он
  эфемерный, это осознанно. `make_cache("drive")` по-прежнему не реализован.
- Номера страниц в находках при заданном диапазоне считаются **от вырезки**,
  а не от исходного PDF (UI об этом предупреждает).
- `MineruError` показывается текстом ошибки, `MineruAuthError` — с подсказкой
  про ключ; трейсбека в UI нет (`result["error"]`).
- В результате конвертации новый ключ `report` (`None` вне режима `mineru`);
  `report.json` скачивается отдельной кнопкой, в общий ZIP не входит.
- Тесты — `tests/test_app_fixes.py`, с фейковым провайдером, без сети. Живой
  прогон MinerU из UI этой правкой **не проверялся** (сети не было): проверены
  путь через локальный кэш на `_test/fixtures/ocr/bakeoff.pdf` и ветка ошибки.

## ocr_converter.py и новый тракт

`ocr_pdf_to_searchable_pdf` — PDF → searchable PDF через `subprocess`
(`ocrmypdf --skip-text --deskew --rotate-pages -l rus+eng`), плюс
`check_ocr_dependencies`. Markdown модуль не производит, в сеть не ходит, кэша
и отчёта у него нет. Оба вызова ограничены по времени: `OCR_TIMEOUT_SEC = 300`,
`DEPENDENCY_TIMEOUT_SEC = 15`.

**Новый тракт его не дублирует и не заменяет.** `ocr_converter.py` остаётся
оффлайн-движком за `OcrmypdfProvider` (в CLI — `--engine ocrmypdf`), MinerU
встаёт рядом вторым провайдером. Файл в автономном прогоне не правится.

## convert.py и сырая HTML-таблица

Прогон: заголовок `## ТЕХНИЧЕСКОЕ ЗАДАНИЕ` + первые 600 символов первого
`<table>` из `vlm.md` (теги закрыты вручную) → `convert_md_to_docx(md, out,
template_path=None)` → открыто `python-docx`.

Результат: **таблица не появилась** — `len(document.tables) == 0`. Весь
фрагмент лёг одним абзацем текста **вместе с тегами**: в тексте абзаца
присутствуют подстроки `<table>`, `<tr>`, `<td>`. Исключения не было.

Вывод для спеки 04: HTML-таблицу из выдачи MinerU обязан разобрать
постпроцессор — до `convert.py` она доходить не должна. Неразобранная таблица
поэтому и стоит правилом `html_table_unparsed` с серьёзностью `critical`.
`convert.py` при этом корректен и не правится: он никогда не заявлял поддержку
сырого HTML.

## Базовый прогон тестов

Интерпретатор важен: системный `python` (`C:\Program Files\Python311`) не
видит `markitdown`, и `python -m pytest -q` из него даёт `4 failed` в
`tests/test_pdf_core.py` — `ModuleNotFoundError: No module named 'markitdown'`,
а не дефект кода. Прогонять нужно проектным `.venv`:

```bash
.venv/Scripts/python.exe -m pytest -q
```

| Состояние | Результат |
|---|---|
| До правок этой спеки, `.venv` | `124 passed, 2 skipped` |
| После правок этой спеки, `.venv` | `129 passed, 2 skipped` |
| Системный Python, любой момент | `4 failed` — нет `markitdown` |

Пять тестов `tests/test_pdf_core.py` требуют `test_files/sample.pdf` и
`test_files/ocr_sample.pdf`; оба файла на месте, поэтому описанный в спеке
базовый прогон без `test_files/` (`3 failed, 2 errors`, все `FileNotFoundError`)
здесь не воспроизводится. Два `skipped` — предсуществующие, к OCR-тракту
отношения не имеют; новый маркер `live` на дату аудита не носит ни один тест.

## Метрика vlm→golden

`VLM_TO_GOLDEN_DIFFS = 76` (ПРАВКА #68; до неё — 12). Расчётное значение в
спеке — 14; **эталон под число не подгонялся**, расхождение объяснимо и
относится к метрике, а не к `golden.md`.

Полный список опкодов `difflib.SequenceMatcher` по `text_tokens`:

| # | Было | Стало | Правка |
|---|---|---|---|
| 1 | `Noп/п` | `№` `п/п` | 1 |
| 2 | `No1` | `№` `1` | 1 |
| 3 | `сыручими` | `сыпучими` | 4 |
| 4 | `$C^{\circ}$` `;-` | `°C;` `-` | 2 + 8 |
| 5 | `$C^{\circ}$` `.` | `°C.` | 2 |
| 6 | `РоЕ` `IR-камерами` | `PoE` `IP-камерами` | 3+5 |
| 7 | `РоЕ;-` | `PoE;` `-` | 3 + 8 |
| 8 | `No384-ФЗ,` | `№` `384-ФЗ,` | 1 |
| 9 | `No7-ФЗ«Об` | `№` `7-ФЗ«Об` | 1 |
| 10 | `A.II.` `Taipov` `P.P.` `Hypeeb` `A.P.` | `А.Ш.` `Таипов` `Р.Р.` `Нуреев` `А.Р.` | 6 |
| 11 | `A.III.` | `А.Ш.` | 6 |
| 12 | `E.K.` | `Е.К.` | 6 |

Причина расхождения 12 против 14 — **опкод 10**. Расчёт исходил из пяти
отдельных опкодов на пять строк блока подписей, но `text_tokens` снимает
переводы строк, и подряд идущие изменённые токены трёх строк (`A.II. Taipov`,
`P.P. Hypeeb` и инициалы `A.P.`) склеиваются в один опкод: первый неизменённый
токен между ними — только фамилия `Сиражитдинов`, стоящая уже после `A.P.`.
Три ожидавшихся опкода схлопнулись в один, 14 − 2 = 12. Остальные одиннадцать
совпали с расчётом один в один, включая предсказание, что склейка трёх
`<table>` в одну pipe-таблицу опкодов не даёт (разметку `text_tokens` снимает).

### ПРАВКА #68: 12 → 76

Правка 8 сборки эталона («;-» и «:-» → «; -» и «: -») разводит 66 склеек на два
токена. 64 из них дают собственный опкод; две попали внутрь уже существовавших
соседних — это опкоды 4 и 7 в таблице выше, их правая колонка поэтому стала
двухсоставной. 12 + 64 = 76. Списки опкодов до и после сверены поштучно:
ни один из двенадцати прежних не исчез и новых, кроме склеек, не появилось.

Остаток тракта на эталоне (`REMAINING_DIFFS` в `tests/test_ocr_postprocess.py`)
от правки 8 не зависит и равен 5: «сыручими», «IR-камерами» и три опкода на пяти
подписях. Порог там опущен с 7 до факта — запас в две единицы пропускал бы
регрессию на один опкод.

### ПРАВКА #72: 76 → 79, остаток 5 → 6

Две новые правки сборки эталона:

- **правка 9** — точка между предложениями (`Ethernet.Для` → `Ethernet. Для`,
  `ПО.Работы` → `ПО. Работы`), 2 опкода. В остаток не попадает: то же делает
  `fix_sentence_glue` в тракте;
- **правка 10** — `(персональныйкомпьютер,` → `(персональный компьютер,`,
  1 опкод. В остаток **попадает**: на `vlm.md` MinerU прислал слово уже слитным
  внутри одной ячейки, а тракт слов не режет. Правильный вариант достижим только
  на сыром прогоне (`vlm_raw.zip`), где та же строка приходит двумя строками
  таблицы и #69 стыкует их поячеечно через пробел.

Уточнение ПРАВКИ #83: разбивка пункта 13 — **не единственное** различие `vlm.md` и сырого
прогона, как следовало из текста выше. Это два разных прогона MinerU: посимвольный дифф
`vlm.md` → `full.md` из `vlm_raw.zip` даёт 24 опкода — 2 на пункт 13 и 22 пробела,
потерянных в сыром прогоне на переносах внутри ячеек.

76 + 2 + 1 = 79; остаток 5 + 1 = 6. Оставшиеся шесть опкодов:
«сыручими» → «сыпучими», `(персональныйкомпьютер,` → `(персональный компьютер,`,
«IR-камерами» → «IP-камерами» и три опкода на пяти подписях.

Эталон закреплён `GOLDEN_SHA256` в `tests/test_ocr_fixtures.py` — файл лежит в
`_test/` (в `.gitignore`), в git его нет, и sha ловит тихую подмену.

Не закрыто и остаётся за человеком (PLACEHOLDER спеки 00): инициалы `Ш.` у
Таипова и Ямалова взяты из второго OCR-прогона (`pipeline.md`), глазами по
стр. 9 `bakeoff.pdf` не сверены. Правка golden по итогам сверки обязана
сопровождаться обновлением `GOLDEN_SHA256`.

## Расхождения с CLAUDE.md / README.md / PLAN_OCR.md

Перечислены, не исправлены — спека 00 править эти файлы не разрешает.

### CLAUDE.md

Пункты 1–3 — **исправлены ПРАВКОЙ #84** (спека 11, 21.09.2026); текст ниже — история.

1. «`convert.py` — Core Markdown → DOCX engine (**1316 lines**)» — фактически
   **1630**.
2. Раздел «Known production limitation» описывает `auto` как единственный
   OCR-путь. После этапов 01–07 появится второй провайдер и CLI; текст
   устареет. Обновляет спека 07 (ей это разрешено).
3. После ПРАВКИ #73 (не исправлено, правка CLAUDE.md в задачу не входила):
   «Numbered edits convention» заканчивает нумерацию на #66 и не знает про #73
   в `app.py`; «Known production limitation» говорит, что `auto` на проде
   падает, — теперь он там просто не предлагается, а рабочий OCR-путь прода —
   MinerU; раздел «Secrets» не упоминает `MINERU_API_KEY`; в описании `app.py`
   нет режима MinerU; `pdf_core.py` описан со старым протоколом
   `ocr_pdf_to_markdown` (с #60 — `ocr_pdf -> OcrResult`).

### docs/specs/ocr/README.md

1. **Исправлено ПРАВКОЙ #84** (кроме PLACEHOLDER 8). После ПРАВКИ #73: этап 8 по-прежнему значится «вне автономного прогона, спек
   нет», а `app.py` — в «Запрещено». Этап 8 выполнен по явному запросу без
   отдельной спеки; таблица «этап → правка» про #73 не знает. PLACEHOLDER 8
   (кэш `drive`) остаётся открытым.

### README.md

1. Строка 72: «ядро Markdown → DOCX (**1316 строк**)» — фактически **1630**
   (та же цифра, что в `CLAUDE.md`; правится одним движением).
2. Строки 72 и 100: сигнатура указана как
   `convert_md_to_docx(md_text, output_filename, template_path=None, images=None)`
   — в коде (`convert.py:1091`) есть ещё параметр `doc_style='pz'`. `CLAUDE.md`
   его упоминает, `README.md` нет.
3. Строка 122: «`conftest.py` — **пустой**, нужен pytest для корневого rootdir»
   — этой спекой в него добавлен маркер `live`, пустым он больше не является.
   С ПРАВКИ #71 маркер включается флагом `MINERU_LIVE=1`: ключ `MINERU_API_KEY`
   живёт в окружении постоянно, и голый `pytest` на нём молча уходил в сеть.
   Прогон без сети — `pytest -m "not live"`, он же прогон по умолчанию.
4. Строки 124–127: список тестов неполон — нет `test_app_fixes.py`,
   `test_golden_pz.py`, `test_letter_style.py`, `test_ocr_converter.py`.
   После этой спеки добавятся ещё `ocr_fixtures.py` и `test_ocr_fixtures.py`.

### docs/PLAN_OCR.md

Поправки, найденные аудитом; в спеках 01–07 уже учтены.

1. Строка 174: «Предпроверка лимитов до загрузки: 200 МБ, **600 страниц**» —
   по доке API MinerU лимит страниц **200**, не 600.
2. Отдельного шага «создание задачи» в API нет: задача ставится сама после PUT.
3. Строка 195, правило склейки таблиц «соседние таблицы с одинаковым числом
   колонок»: вторая из трёх разорванных таблиц в `vlm.md` имеет **2 колонки**,
   а не 3 — на ней правило не сработает. Рабочее правило склейки — в спеке 04.
4. `РоЕ` в `vlm.md` целиком кириллический, это не «смешанный алфавит внутри
   токена» — правилом `mixed_alphabet_unknown` он не ловится.
5. В блоке подписей 6 должностей и 5 ФИО — пары не восстанавливаются, поэтому
   `signature_block` только помечает разрыв, а не чинит его.
6. «сыручими» и «IR-камерами» (строки 92–93) правилами не ловятся вообще —
   только сверкой двух прогонов, спека 06.
7. `ocr_auto_mode.py` протокол провайдера не использует — этап 1 его не трогает.

## История коммитов: почему #74–#78 одним коммитом (2026-09-20)

К началу сессии ПРАВКИ #78–#80 рабочее дерево было грязным: HEAD стоял на #73,
а изменения #74–#77 (`recover_dropped_blocks`, склейки #75, фикстура
`vlm_raw2.zip`, прозаический `ГОСТ` #77) лежали незакоммиченными в тех же
файлах. ПРАВКА #78 меняет `_dropped_groups` — функцию, целиком добавленную
#74 и ещё не попавшую в историю, поэтому отдельного диффа у неё нет: в
`git diff` относительно HEAD её строки неотделимы от строк #74.

Отсюда фактическая история:

- один коммит `ПРАВКА #74–#78` — `ocr/postprocess.py`, `ocr/cli.py`,
  `tests/ocr_fixtures.py`, `tests/test_ocr_diff.py`,
  `tests/test_ocr_fixtures.py`, `tests/test_ocr_postprocess.py`;
- `ПРАВКА #79` — `ocr/validate.py` и её тесты; тянет за собой хвост #77 из
  того же файла (прозаический `ГОСТ`, тире трёх видов) по той же причине;
- `ПРАВКА #80` — `ocr/validate.py`, тесты и спека 05.

Перед каждым коммитом прогон `pytest -q` был зелёным на том состоянии дерева,
которое коммитится (238, 242 и 244 теста соответственно).

## Фикстуры этапа A (2026-09-20)

Шаг 0 спеки 09: ревизия сырых прогонов в `_test/fixtures/ocr/` против `.cache/ocr/`.
Сеть не понадобилась: все четыре zip на месте, под ожидаемыми именами, замен не было.

| Фикстура | Сырой выход | sha256 PDF (начало) | meta.sha256 = sha | zip побайтно = кэш | Страниц PDF / content_list |
|---|---|---|---|---|---|
| `bakeoff.pdf` | `vlm_raw.zip` | `0e891b731882` | да | да | 9 / 9 |
| `bakeoff2.pdf` | `vlm_raw2.zip` | `d1e252137064` | да | да | 8 / 8 |
| `bakeoff3.pdf` | `vlm_raw3.zip` | `24fdb4517e6e` | да | да | 9 / 9 |
| `textpdf1.pdf` | `vlm_raw_textpdf1.zip` | `ad6c89f0c72f` | да | да | 10 / 10 |
| `docx1.docx` | — (родная конвертация) | — | — | — | файл на месте |
| `xlsx1.xlsx` | — (родная конвертация) | — | — | — | файл на месте |

Запись кэша — `{sha}-mineru-vlm-pall-v1` (`.zip` + `.meta.json`). В каждом zip есть
`full.md` и ровно один `*content_list.json`. Дальше тракт этапа A работает оффлайн.

### ПРАВКА #81

`ocr/ingest.py` — единый вход `ingest(data, source_name=…, work_dir=…, …)` для `.pdf` / `.docx` / `.xlsx`.
`detect_route`: `scan` (хотя бы одна страница без слоя, в т.ч. смешанный PDF), `text_tables`
(слой везде + `pdf_has_tables` на `pdfplumber.find_tables`, линии), `text`, `office`.
`scan` и `text_tables` — один вызов `run_pipeline`; `text` и `office` — MarkItDown, затем тот же
`postprocess` + `validate` + `build_report` (`provider="markitdown"`, `model_version=None`,
`page=None`). `--verify` на маршрутах MarkItDown — `ValueError`. `ocr/cli.py`: `main` зовёт `ingest`,
`run_pipeline` не тронут, `app.py` зовёт его как раньше.

Правка приёмки 21.09.2026: `test_files/sample.pdf` — смешанный PDF (13-я из 13 страниц без слоя),
детектор даёт `scan`; маршрут `text` в тестах закрыт синтетической вырезкой страниц 1–2
`textpdf1.pdf` (`tests/test_ocr_ingest.py::two_page_text_pdf`), постоянной фикстуры на него нет.
### ПРАВКА #82

`ocr/board.py` — табло качества `python -m ocr.board`, строго оффлайн. `build_board` кладёт
`vlm_raw*.zip` во временный `LocalCache` и зовёт `ingest` с `provider_factory=_offline`
(промах кэша — `RuntimeError`, рабочий `.cache/ocr/` не читается и не пишется). Строка табло —
закрытый набор из 14 ключей, от маршрута не зависит; `count_diffs` / `threshold` — `null` до спеки 10.
`main` пишет `_test/quality_board.json`, `_test/board/<stem>/out.md` + `report.json` и пять заготовок
`<stem>.errors.txt` (у `bakeoff.pdf` — `golden.md`); существующий `errors.txt` не перезаписывается.

Табло на 2026-09-21:

| fixture | route | critical | warning | info | tables | table_broken | chars |
|---|---|---|---|---|---|---|---|
| `bakeoff.pdf` | scan | 5 | 7 | 2 | 1 | 0 | 20956 |
| `bakeoff2.pdf` | scan | 2 | 3 | 2 | 1 | 0 | 17271 |
| `bakeoff3.pdf` | scan | 0 | 3 | 8 | 5 | 0 | 20148 |
| `textpdf1.pdf` | text_tables | 0 | 77 | 12 | 8 | 0 | 19554 |
| `docx1.docx` | office | 0 | 1 | 0 | 4 | 0 | 6700 |
| `xlsx1.xlsx` | office | 0 | 4 | 0 | 1 | 0 | 7636 |

### ПРАВКА #83

`ocr/board.py`: `parse_errors` / `apply_errors` — эталон фикстуры = черновик тракта + закрытый список
правок из `<stem>.errors.txt` («было» обязано встречаться в тексте ровно один раз, правки идут
последовательно); `python -m ocr.board --golden [--force]` пишет пять `<stem>.golden.md`, существующий
без `--force` не трогает, `golden.md` бейкоффа не трогает никогда. `text_tokens` / `count_diffs`
переехали в `ocr/board.py`, в `tests/ocr_fixtures.py` — реэкспорт. Строка табло получила
`count_diffs`, `threshold`, `per_1000_tokens` (15 ключей; без файла эталона — все три `null`).

Эталоны на 2026-09-21 (все пять `errors.txt` непустые — **якорей, то есть фикстур с пустым списком, нет**):

| фикстура | маршрут | строк в errors.txt | `count_diffs` = порог | на 1000 токенов | sha эталона |
|---|---|---|---|---|---|
| `bakeoff.pdf` | scan | — (спека 00, правки 1–11) | 13 | 5.1 | `86987973e159` |
| `bakeoff2.pdf` | scan | 20 | 20 | 9.6 | `fe0d3f8886de` |
| `bakeoff3.pdf` | scan | 8 | 8 | 3.4 | `65d67c905b32` |
| `textpdf1.pdf` | text_tables | 14 | 13 | 5.8 | `195d349e1c17` |
| `docx1.docx` | office | 7 | 8 | 9.2 | `317e9138abdb` |
| `xlsx1.xlsx` | office | 1 | 1 | 1.1 | `1f019aef6a77` |

Верхняя граница «строк × 3» не превышена ни у одной фикстуры. Все «было» нашлись в свежем
черновике ровно по одному разу, `errors.txt` не правились.

#### `golden.md` бейкоффа: правка 11 спеки 00

В эталон вставлена строка `с. Сафарово, 2026 г.` — нижний колонтитул титульного листа (на скане
есть, сверено человеком 21.09.2026; в `content_list.json` — `type: footer`, `page_idx: 0`; в
`full.md` / `vlm.md` её нет). Стоит отдельным абзацем вплотную перед `| № п/п | …` — ровно там,
куда её ставит `recover_dropped_blocks` на сыром пути. Больше в файле ничего не менялось; sha
`f96c73ac39b3…` → `86987973e159…`.

Сырой путь (`vlm_raw.zip`) против эталона, 14 → 13 — опкоды до и после:

| # | Было (тракт) | Стало (эталон) | до | после | Класс |
|---|---|---|---|---|---|
| 1 | `с.` `Сафарово,` `2026` `г.` | — (delete) | есть | **ушёл** | строка из `content_list`, эталон её не содержал |
| 2 | `сыручими` | `сыпучими` | есть | есть | только сверкой прогонов |
| 3 | `комплексом).В` | `комплексом).` `В` | есть | есть | склейка MinerU в ячейке |
| 4 | `комплекса)Предусмотреть` | `комплекса)` `Предусмотреть` | есть | есть | то же |
| 5 | `1СКомпонента` | `1С` `Компонента` | есть | есть | то же |
| 6 | `помещений-` | `помещений` `-` | есть | есть | то же |
| 7 | `помещениях-` | `помещениях` `-` | есть | есть | то же |
| 8 | `IP65не` | `IP65` `не` | есть | есть | то же |
| 9 | `IP54не` | `IP54` `не` | есть | есть | то же |
| 10 | `проектомВыдаются` | `проектом` `Выдаются` | есть | есть | то же |
| 11 | `IR-камерами` | `IP-камерами` | есть | есть | только сверкой прогонов |
| 12 | `A.II.` `Taipov` `P.P.` `Hypeeb` `A.P.` | `А.Ш.` `Таипов` `Р.Р.` `Нуреев` `А.Р.` | есть | есть | подписи |
| 13 | `A.III.` | `А.Ш.` | есть | есть | подписи |
| 14 | `E.K.` | `Е.К.` | есть | есть | подписи |

Раскладка 14 относительно остатка пути `postprocess(vlm.md)` (6): 6 − 1 (`персональныйкомпьютер`
на сыром пути чинит #69) + 1 (Сафарово) + 8 (склейки 3–10) = 14. Восемь склеек — пункты 13,
13.1, 14: в сыром прогоне потеряно 22 пробела, тракт вернул 14, данных для остальных в архиве
нет; правилом не берутся, материал этапа B. Порог 13 — факт, не запас; assert «остаток ≤ 6» из
теста табло убран — он про вход `vlm.md` и живёт в `tests/test_ocr_postprocess.py`.

Путь `postprocess(vlm.md)`: `REMAINING_DIFFS` 6 → 7. Прежние шесть опкодов на месте, добавился
один — `insert` `с.` `Сафарово,` `2026` `г.`: без `content_list` тракту строку взять неоткуда.

`VLM_TO_GOLDEN_DIFFS` — 99, **не изменился**. В эталоне строка Сафарово стоит вплотную перед
`№ п/п`, а на пути `vlm.md` → эталон этот токен и так менялся опкодом
`replace ['Noп/п'] -> ['№', 'п/п']`. Четыре новых токена примыкают к нему без единого общего
токена между ними, и `difflib` расширяет тот же `replace`
(`['Noп/п'] -> ['с.', 'Сафарово,', '2026', 'г.', '№', 'п/п']`), отдельного `insert` не создаёт.
На пути `postprocess(vlm.md)` токен `Noп/п` уже исправлен трактом, сливаться не с чем — отсюда там +1.

Про 79 → 99: выше в разделе «Метрика vlm→golden» запись кончается на 79 (#72). 99 набежало на
пересборке `golden.md` при #74–#78 (расклейка номеров пунктов, 19 опкодов, и `1.План`, 1 опкод —
по комментарию в `tests/test_ocr_fixtures.py`). Прежний эталон в git не хранится (`_test/` в
`.gitignore`), поэтому поопкодного разложения этой дельты нет и не будет — **оно не требуется и
не выдумывается**. С #83 метрика ведётся от 99, и впредь каждая пересборка эталона
сопровождается списком опкодов до и после в этом файле (как на #68, #72 и здесь).

Заметка для этапа C (в этой спеке не чинится): на `IP65не` / `IP54не` правило
`mixed_alphabet_unknown` подсказывает украинскую «І» вместо пропущенного пробела — ложная подсказка.

### ПРАВКА #84

Гигиена после этапа A (спека 11). `ocr/board.py`: `parse_errors_numbered` отдаёт
`(номер строки файла, (страница, было, надо))`, `parse_errors` — обёртка над ним; `apply_errors`
получил необязательный именованный `lines` — с ним ошибка 0/2 вхождений начинается с
`строка N errors.txt:`, без него текст прежний; `write_goldens` добавляет впереди имя файла
(`xlsx1.errors.txt: строка N errors.txt: «…»: вхождений 0, нужно ровно 1`). Сигнатуры спеки 10 целы;
тракт, эталоны, sha и пороги не менялись — табло те же шесть строк.

`requirements.txt`: `streamlit==1.58.0` (разрешено человеком 21.09.2026; версия — та, что стоит в `.venv`).
Транзитивные зависимости плавают.

Документы: ретроспективная спека `08-ui-mineru.md` (#73) по коду `app.py`; `CLAUDE.md` сверен по
закрытому списку из девяти пунктов; в `docs/specs/ocr/README.md` — строки 08 и 11, три правила
(`recovered_block`, `code_digits_glued`, `org_name_variant`), оговорка про `app.py`; `PLAN_OCR.md` — по факту этапа A.

PLACEHOLDER-ы MinerU 1 и 2 закрыты **в документах** по доке API (прочитана агентом-исследователем,
руками не перепроверена): код квоты `-60018`, `page_ranges` — поле элемента `files[]`. В
`ocr/mineru_provider.py` `QUOTA_CODES` пуст и оба комментария `# PLACEHOLDER` на месте — до отдельной
правки провайдера (решение человека 21.09.2026). Лимит страниц (200 / 600) и код «слишком частые
запросы» — не сверены.

Расхождения, найденные при сверке сверх списка спеки (не чинились):

1. `README.md` корня, пункты 1–4 раздела «README.md» выше — по-прежнему открыты (спека 11 файл не трогает).
2. `CLAUDE.md`, раздел «Working principles»: «Keep it under ~200 lines» — файл около 245 строк;
   спека 11 держит его «в пределах ~250».
3. `docs/specs/ocr/02-mineru-provider.md`, блок кода «Интерфейсы (дословно)»: комментарий
   `# PLACEHOLDER: код «квота исчерпана» неизвестен` у `QUOTA_CODES` оставлен — он дословно повторяет код провайдера.

**Снимок нумерации (2026-09-21).** В коде `#1…#24, #26…#84`. Следующий свободный номер — **#85**.
