# Синхронизация документации с кодом (аудит 2026-09-18)

Результат этапа 0 спек OCR-тракта (`docs/specs/ocr/00-audit.md`). Продуктовый
код не менялся — здесь только факты, перепроверенные по репозиторию, и список
мест, где `CLAUDE.md` / `README.md` / `docs/PLAN_OCR.md` разошлись с кодом.
Сами эти файлы спека 00 править не разрешает.

## Модули

Семь продуктовых модулей в корне, `conftest.py` восьмой файл — тестовая
инфраструктура, не модуль приложения. Число строк — `wc -l` на дату аудита.

| Файл | Строк | Роль |
|---|---|---|
| `convert.py` | 1630 | Markdown → DOCX, монолит |
| `app.py` | 728 | Streamlit UI, два режима |
| `file_converter.py` | 712 | DOCX/PDF/TXT → Markdown, слой MarkItDown, `analyze_pdf_pages` |
| `ocr_converter.py` | 185 | subprocess-обёртка `ocrmypdf` |
| `pdf_core.py` | 115 | провайдеро-независимое ядро PDF → Markdown |
| `ocr_auto_mode.py` | 81 | оркестратор «OCR или нет» |
| `markdown_cleanup.py` | 32 | детерминированная чистка OCR-Markdown |
| `conftest.py` | 23 | маркер `live`; с ПРАВКИ #71 включается флагом `MINERU_LIVE=1` |

Зависимости: `requests` в `requirements.txt` **нет**; в `.venv` он стоит
транзитивно (2.34.2), поэтому локально импортируется и без правки файла. Строку
`requests` добавляет спека 02, не эта.

`markdown_cleanup.cleanup_ocr_markdown` ни к чему не подключён (ни UI, ни
тракт), покрыт `tests/test_markdown_cleanup.py`. Из его правил для OCR-тракта
важна одна: `\bNo\.?\s*(?=\d)` → `№ ` (с пробелом). `Noп/п` она не ловит —
после `No` там буква, а не цифра.

## Нумерация правок

Последняя правка в коде — **#59**, `app.py:284`. Сплошной набор
`#1…#24, #26…#59`; **#25 отсутствует** и никогда не существовал — номер
пропущен навсегда, задним числом не присваивается. Проверка:

```bash
grep -rn "ПРАВКА #" --include=*.py . | grep -v '/\.venv/'
```

Следующий свободный номер — **#60**, он закреплён за спекой 01. Распределение
#60…#66 по спекам — в `docs/specs/ocr/README.md`.

## Контракт OcrProvider

`pdf_core.py:16-20`, дословно:

```python
class OcrProvider(Protocol):
    """Интерфейс OCR-провайдера: скан-PDF (bytes) -> markdown."""

    def ocr_pdf_to_markdown(self, pdf_bytes: bytes,
                            page_range: str | None = None) -> str: ...
```

Единственная реализация — `OcrmypdfProvider` (`pdf_core.py:23`). `page_range`
имеет умолчание `None`, то есть реализации обязаны принимать вызов и без него.

## Как вызывается OCR-тракт

```
app._convert_uploaded_file (app.py:245)
  └─ pdf_core.pdf_to_markdown_with_status(bytes, page_range=…, mode=…)
       → (str, dict | None)
```

Сигнатуры (`pdf_core.py:43`, `pdf_core.py:84`):

```python
def pdf_to_markdown_with_status(pdf_bytes, *, page_range=None, mode="auto",
                                provider=None) -> tuple[str, dict[str, Any] | None]
def pdf_to_markdown(pdf_bytes, *, page_range=None, mode="auto", provider=None) -> str
```

Аргумент `provider` из UI **не передаётся никогда** — `app.py` зовёт функцию
только с `page_range` и `mode`. Единственные потребители протокола на момент
аудита — тесты (`tests/test_pdf_core.py`). Поэтому подключение нового
провайдера к UI — отдельный этап 8, а не побочный эффект этапов 01–07.

`ocr_auto_mode.py` протокол провайдера не использует вообще: слов `provider` и
`Protocol` в файле нет. Этап 1 его не трогает.

## ocr_converter.py и новый тракт

`ocr_pdf_to_searchable_pdf` — PDF → searchable PDF через `subprocess`
(`ocrmypdf --skip-text --deskew --rotate-pages -l rus+eng`), плюс
`check_ocr_dependencies`. Markdown модуль не производит, в сеть не ходит, кэша
и отчёта у него нет. Оба вызова ограничены по времени: `OCR_TIMEOUT_SEC = 300`,
`DEPENDENCY_TIMEOUT_SEC = 15`.

**Новый тракт его не дублирует и не заменяет.** `ocr_converter.py` остаётся
оффлайн-движком за `OcrmypdfProvider` (в CLI — `--engine ocrmypdf`), MinerU
встаёт рядом вторым провайдером. Файл в автономном прогоне не правится.

## convert.py и сырая HTML-таблица

Прогон: заголовок `## ТЕХНИЧЕСКОЕ ЗАДАНИЕ` + первые 600 символов первого
`<table>` из `vlm.md` (теги закрыты вручную) → `convert_md_to_docx(md, out,
template_path=None)` → открыто `python-docx`.

Результат: **таблица не появилась** — `len(document.tables) == 0`. Весь
фрагмент лёг одним абзацем текста **вместе с тегами**: в тексте абзаца
присутствуют подстроки `<table>`, `<tr>`, `<td>`. Исключения не было.

Вывод для спеки 04: HTML-таблицу из выдачи MinerU обязан разобрать
постпроцессор — до `convert.py` она доходить не должна. Неразобранная таблица
поэтому и стоит правилом `html_table_unparsed` с серьёзностью `critical`.
`convert.py` при этом корректен и не правится: он никогда не заявлял поддержку
сырого HTML.

## Базовый прогон тестов

Интерпретатор важен: системный `python` (`C:\Program Files\Python311`) не
видит `markitdown`, и `python -m pytest -q` из него даёт `4 failed` в
`tests/test_pdf_core.py` — `ModuleNotFoundError: No module named 'markitdown'`,
а не дефект кода. Прогонять нужно проектным `.venv`:

```bash
.venv/Scripts/python.exe -m pytest -q
```

| Состояние | Результат |
|---|---|
| До правок этой спеки, `.venv` | `124 passed, 2 skipped` |
| После правок этой спеки, `.venv` | `129 passed, 2 skipped` |
| Системный Python, любой момент | `4 failed` — нет `markitdown` |

Пять тестов `tests/test_pdf_core.py` требуют `test_files/sample.pdf` и
`test_files/ocr_sample.pdf`; оба файла на месте, поэтому описанный в спеке
базовый прогон без `test_files/` (`3 failed, 2 errors`, все `FileNotFoundError`)
здесь не воспроизводится. Два `skipped` — предсуществующие, к OCR-тракту
отношения не имеют; новый маркер `live` на дату аудита не носит ни один тест.

## Метрика vlm→golden

`VLM_TO_GOLDEN_DIFFS = 76` (ПРАВКА #68; до неё — 12). Расчётное значение в
спеке — 14; **эталон под число не подгонялся**, расхождение объяснимо и
относится к метрике, а не к `golden.md`.

Полный список опкодов `difflib.SequenceMatcher` по `text_tokens`:

| # | Было | Стало | Правка |
|---|---|---|---|
| 1 | `Noп/п` | `№` `п/п` | 1 |
| 2 | `No1` | `№` `1` | 1 |
| 3 | `сыручими` | `сыпучими` | 4 |
| 4 | `$C^{\circ}$` `;-` | `°C;` `-` | 2 + 8 |
| 5 | `$C^{\circ}$` `.` | `°C.` | 2 |
| 6 | `РоЕ` `IR-камерами` | `PoE` `IP-камерами` | 3+5 |
| 7 | `РоЕ;-` | `PoE;` `-` | 3 + 8 |
| 8 | `No384-ФЗ,` | `№` `384-ФЗ,` | 1 |
| 9 | `No7-ФЗ«Об` | `№` `7-ФЗ«Об` | 1 |
| 10 | `A.II.` `Taipov` `P.P.` `Hypeeb` `A.P.` | `А.Ш.` `Таипов` `Р.Р.` `Нуреев` `А.Р.` | 6 |
| 11 | `A.III.` | `А.Ш.` | 6 |
| 12 | `E.K.` | `Е.К.` | 6 |

Причина расхождения 12 против 14 — **опкод 10**. Расчёт исходил из пяти
отдельных опкодов на пять строк блока подписей, но `text_tokens` снимает
переводы строк, и подряд идущие изменённые токены трёх строк (`A.II. Taipov`,
`P.P. Hypeeb` и инициалы `A.P.`) склеиваются в один опкод: первый неизменённый
токен между ними — только фамилия `Сиражитдинов`, стоящая уже после `A.P.`.
Три ожидавшихся опкода схлопнулись в один, 14 − 2 = 12. Остальные одиннадцать
совпали с расчётом один в один, включая предсказание, что склейка трёх
`<table>` в одну pipe-таблицу опкодов не даёт (разметку `text_tokens` снимает).

### ПРАВКА #68: 12 → 76

Правка 8 сборки эталона («;-» и «:-» → «; -» и «: -») разводит 66 склеек на два
токена. 64 из них дают собственный опкод; две попали внутрь уже существовавших
соседних — это опкоды 4 и 7 в таблице выше, их правая колонка поэтому стала
двухсоставной. 12 + 64 = 76. Списки опкодов до и после сверены поштучно:
ни один из двенадцати прежних не исчез и новых, кроме склеек, не появилось.

Остаток тракта на эталоне (`REMAINING_DIFFS` в `tests/test_ocr_postprocess.py`)
от правки 8 не зависит и равен 5: «сыручими», «IR-камерами» и три опкода на пяти
подписях. Порог там опущен с 7 до факта — запас в две единицы пропускал бы
регрессию на один опкод.

Эталон закреплён `GOLDEN_SHA256` в `tests/test_ocr_fixtures.py` — файл лежит в
`_test/` (в `.gitignore`), в git его нет, и sha ловит тихую подмену.

Не закрыто и остаётся за человеком (PLACEHOLDER спеки 00): инициалы `Ш.` у
Таипова и Ямалова взяты из второго OCR-прогона (`pipeline.md`), глазами по
стр. 9 `bakeoff.pdf` не сверены. Правка golden по итогам сверки обязана
сопровождаться обновлением `GOLDEN_SHA256`.

## Расхождения с CLAUDE.md / README.md / PLAN_OCR.md

Перечислены, не исправлены — спека 00 править эти файлы не разрешает.

### CLAUDE.md

1. «`convert.py` — Core Markdown → DOCX engine (**1316 lines**)» — фактически
   **1630**.
2. Раздел «Known production limitation» описывает `auto` как единственный
   OCR-путь. После этапов 01–07 появится второй провайдер и CLI; текст
   устареет. Обновляет спека 07 (ей это разрешено).

### README.md

1. Строка 72: «ядро Markdown → DOCX (**1316 строк**)» — фактически **1630**
   (та же цифра, что в `CLAUDE.md`; правится одним движением).
2. Строки 72 и 100: сигнатура указана как
   `convert_md_to_docx(md_text, output_filename, template_path=None, images=None)`
   — в коде (`convert.py:1091`) есть ещё параметр `doc_style='pz'`. `CLAUDE.md`
   его упоминает, `README.md` нет.
3. Строка 122: «`conftest.py` — **пустой**, нужен pytest для корневого rootdir»
   — этой спекой в него добавлен маркер `live`, пустым он больше не является.
   С ПРАВКИ #71 маркер включается флагом `MINERU_LIVE=1`: ключ `MINERU_API_KEY`
   живёт в окружении постоянно, и голый `pytest` на нём молча уходил в сеть.
   Прогон без сети — `pytest -m "not live"`, он же прогон по умолчанию.
4. Строки 124–127: список тестов неполон — нет `test_app_fixes.py`,
   `test_golden_pz.py`, `test_letter_style.py`, `test_ocr_converter.py`.
   После этой спеки добавятся ещё `ocr_fixtures.py` и `test_ocr_fixtures.py`.

### docs/PLAN_OCR.md

Поправки, найденные аудитом; в спеках 01–07 уже учтены.

1. Строка 174: «Предпроверка лимитов до загрузки: 200 МБ, **600 страниц**» —
   по доке API MinerU лимит страниц **200**, не 600.
2. Отдельного шага «создание задачи» в API нет: задача ставится сама после PUT.
3. Строка 195, правило склейки таблиц «соседние таблицы с одинаковым числом
   колонок»: вторая из трёх разорванных таблиц в `vlm.md` имеет **2 колонки**,
   а не 3 — на ней правило не сработает. Рабочее правило склейки — в спеке 04.
4. `РоЕ` в `vlm.md` целиком кириллический, это не «смешанный алфавит внутри
   токена» — правилом `mixed_alphabet_unknown` он не ловится.
5. В блоке подписей 6 должностей и 5 ФИО — пары не восстанавливаются, поэтому
   `signature_block` только помечает разрыв, а не чинит его.
6. «сыручими» и «IR-камерами» (строки 92–93) правилами не ловятся вообще —
   только сверкой двух прогонов, спека 06.
7. `ocr_auto_mode.py` протокол провайдера не использует — этап 1 его не трогает.
